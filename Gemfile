source "https://rubygems.org"

# GitHub Pages gem dependencies
gem "github-pages", "~> 232", group: :jekyll_plugins

# Windows and JRuby
platforms :mswin, :jruby do
  gem "wdm", "~> 0.1.0", :platforms => [:mswin, :jruby]
end
